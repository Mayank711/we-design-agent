Selectable chip for filtering loads by vehicle type, origin, etc. Selected = green tint + green border.

```jsx
<FilterChip selected={sel === 'open'} onClick={() => set('open')}>Open body</FilterChip>
```
