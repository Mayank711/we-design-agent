Violet gradient nudge pill — surfaces bid-maximisation and premium hints. Uses Poppins (not Baloo) to match the product.

```jsx
<PremiumPill>Increase bid to win more loads</PremiumPill>
```

Defaults to a ⚡ thunder glyph; override with `icon`.
